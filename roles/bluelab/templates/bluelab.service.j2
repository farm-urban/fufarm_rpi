[Unit]
Description=bluelab
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=exec
ExecStart=/home/{{ansible_user}}/Bluelab/Connect/connect
Restart=always

[Install]
WantedBy=graphical-session.target