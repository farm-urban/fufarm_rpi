---
- name: Setup access-point/client
  hosts: local
  remote_user: pi
  become: yes # remote_user and/or become?
  vars_files:
    - files/variables.yml
  roles:
    # - rpi-setup
    - ap-client
  # tasks:
  # - name: Print the gateway for each host when defined
  #   ansible.builtin.debug:
  #     msg: ansible_distribution_release {{ ansible_distribution_release }}

